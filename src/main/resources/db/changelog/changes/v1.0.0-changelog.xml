<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <changeSet id="1" author="petr">
        <sql>
            CREATE SCHEMA IF NOT EXISTS smart_budget;
        </sql>
    </changeSet>

    <changeSet id="2" author="petr">
        <sql>
            CREATE TABLE IF NOT EXISTS smart_budget.users (
                                                              id BIGINT PRIMARY KEY,
                                                              username VARCHAR(255) NOT NULL,
                password VARCHAR(255) NOT NULL,
                account_balance DECIMAL,
                created_at TIMESTAMP NOT NULL,
                updated_at TIMESTAMP NOT NULL
                );
        </sql>
    </changeSet>

    <changeSet id="3" author="petr">
        <sql>
            CREATE TABLE IF NOT EXISTS smart_budget.transactions (
                                                                     id BIGINT PRIMARY KEY,
                                                                     transaction_sum DECIMAL,
                                                                     category VARCHAR(255),
                description VARCHAR(255),
                bank_id BIGINT,
                created_at TIMESTAMP NOT NULL,
                user_id BIGINT,
                cash_back_sum DECIMAL
                );
        </sql>
    </changeSet>

</databaseChangeLog>